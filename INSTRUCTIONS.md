# マインスイーパー実装指示書

## 実装要件

### 1. 基本機能
- **盤面の可変サイズ**: 縦横のサイズを自由に設定可能
- **地雷数の設定**: 盤面サイズに応じて地雷数を調整可能
- **初回クリック時の盤面生成**: ゲーム開始時ではなく、最初のセルをクリックした瞬間に盤面を生成
  - 初回クリックしたセルとその周囲8マスには地雷を配置しない
- **素材の活用**: `/src/assets/game` にある画像素材を配置してUIを構築

### 2. ゲームルール
- **通常のマインスイーパールール**:
  - 左クリックでセルを開く
  - 右クリックで旗を立てる/外す
  - 数字は周囲8マスの地雷数を示す
  - 地雷のないセルを全て開くと勝利
  - 地雷を開くとゲームオーバー

- **追加仕様: 周囲展開機能**:
  - すでに開かれているセルを再度クリックした場合
  - そのセルの数字と周囲の旗の数が一致している場合
  - 周囲の未開封セル（旗が立っていないセル）を全て自動で開く
  - この機能により、確実に旗を立てた後の効率的なプレイが可能

### 3. 使用する素材ファイル

#### セルの状態表示
- `closed.svg`: 未開封のセル
- `flag.svg`: 旗が立てられたセル
- `type0.svg` ~ `type8.svg`: 周囲の地雷数を示す開封済みセル
- `mine.svg`: 地雷（ゲームオーバー時に表示）
- `mine_red.svg`: クリックして踏んだ地雷（ゲームオーバーの原因となった地雷）

#### UI装飾（オプション）
- `border_hor_2x.png`: 水平方向のボーダー
- `border_vert_2x.png`: 垂直方向のボーダー
- `d0.svg` ~ `d9.svg`: 数字表示用（カウンター等に使用可能）

### 4. 技術スタック
- **フレームワーク**: React + TypeScript
- **ビルドツール**: Vite
- **デスクトップアプリ**: Tauri 2.0

### 5. 実装の詳細

#### 状態管理
- `board`: 盤面の状態（各セルの情報を保持）
- `gameOver`: ゲームオーバーフラグ
- `gameWon`: 勝利フラグ
- `firstClick`: 初回クリックフラグ

#### セルの構造
```typescript
type Cell = {
  isMine: boolean;        // 地雷かどうか
  state: CellState;       // セルの状態（閉じている/開いている/旗）
  adjacentMines: number;  // 周囲8マスの地雷数
};
```

#### 主要な関数
- `initializeBoard(clickedRow, clickedCol)`: 初回クリック時に盤面を生成
- `openCell(r, c, board)`: セルを開く（再帰的に空きセルを展開）
- `handleCellClick(r, c)`: セルクリック時の処理（通常開封 + 周囲展開）
- `handleCellRightClick(e, r, c)`: 右クリックで旗を立てる/外す
- `checkWin(board)`: 勝利条件の判定

### 6. UIレイアウト
- 上部: タイトル「Minesweeper」
- 設定画面（ゲーム開始前）:
  - 縦のマス数入力
  - 横のマス数入力
  - 地雷数入力
  - 説明テキスト
- ゲーム画面:
  - グリッド表示の盤面
  - ステータス表示（Game Over / You Win）
  - New Gameボタン

### 7. スタイリング
- セルサイズ: 32px × 32px
- グリッドレイアウトで盤面を表示
- ダークモード対応
- レスポンシブ対応

## 動作確認手順
1. `pnpm install` で依存関係をインストール
2. `pnpm run dev` で開発サーバーを起動
3. ブラウザでゲームをテスト
4. `pnpm run tauri dev` でデスクトップアプリとして起動

## 注意点
- 初回クリック位置の周囲には地雷を配置しないことで、プレイヤーが必ず安全に開始できる
- 周囲展開機能は上級者向けの効率化機能として実装
- 右クリックのコンテキストメニューを無効化（`preventDefault`）
